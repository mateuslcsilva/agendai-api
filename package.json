{
  "name": "agendai-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "git pull origin main && tsc && node src/server.js",
    "conflict": "git diff --name-only --diff-filter=U --relative"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tsx": "^4.7.1",
    "typescript": "^5.4.3"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@fastify/mysql": "^4.2.0",
    "@types/node": "^20.11.30",
    "fastify": "^4.26.2"
  }
}
